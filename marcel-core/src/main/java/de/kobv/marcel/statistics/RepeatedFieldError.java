package de.kobv.marcel.statistics;

import de.kobv.marcel.beans.Record;

/**
 */
public class RepeatedFieldError extends AbstractRecordProcessor {

    @Override
    public void process(final Record record) {
        /*List<Controlfield> fields = record.getControlFields("007");
        if (fields != null && fields.size() > 1) {
            // TODO error found
        }*/
    }

}
